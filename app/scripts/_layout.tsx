import { Stack } from 'expo-router';

export default function ScriptsLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
